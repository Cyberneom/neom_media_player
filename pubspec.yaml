name: neom_media_player
description: Mediaplayer for functionalities like playing audio, video, and youtube videos.
version: 1.0.0
homepage: https://github.com/Cyberneom/neom_media_player
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  neom_commons:
    git:
      url: git@github.com:Cyberneom/neom_commons.git

  audio_service: ^0.18.18
  audioplayers: ^6.4.0
  video_player: ^2.9.5
  youtube_player_flutter: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
